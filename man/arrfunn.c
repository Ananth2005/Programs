#include<stdio.h>
int size;
void (int arr[])
{
    int i;
    for(i=0;i<size;i++)
        printf("%d\t",i);
}
int main()
{
    int a[30],i;
    printf("enter the elements of numper: ");
    scanf("%d",&size);
    printf("enter the value: ");
    for(i=0;i<size;i++)
        scanf("%d",&a[i]);
       // fun (a);
}
